# 迭代说明：<需求摘要>

> 本文件是**本次会话的工作区索引**，不是台账。进度状态仅用于断点恢复，迭代结束可整目录删除。
> 状态值只有 `pending` / `doing` / `done` / `blocked`，均为执行态，不是审批态。

## 基本信息

| 项 | 值 |
|---|---|
| 迭代 ID | `<YYYYMMDD>-<slug>` |
| 创建时间 | `<YYYY-MM-DD HH:MM>` |
| 需求来源 | `<用户描述 / 文件路径 / PRD / HTML 原型>` |
| 迭代目录 | `.task/<YYYYMMDD>-<slug>/` |
| 工作分支 | `codex/<slug>` |

## 仓库与基线

| 项 | 值 |
|---|---|
| repo root | `<git rev-parse --show-toplevel>` |
| 起始分支 | `<git rev-parse --abbrev-ref HEAD>` |
| 起始 HEAD | `<git log -1 --format=%H>` |
| 起始工作区 | `<git status --porcelain 摘要，或「干净」>` |
| 既有改动处理 | `<保留为基线 / 无>` |

## 复杂度判定

| 信号 | 是否命中 | 证据 |
|---|---|---|
| `<A1~A5 / B1~B5>` | `<是 / 否>` | `<具体表名、文件、接口、模块>` |

**判定结果**：`<较大 / 小>` → 确认关卡 `<3 次 / 1 次>`

**用户覆盖**：`<无 / 用户原话摘要>`

> 阶段 2 完成后需复核一次；只允许"小 → 大"升级。

## 阶段进度

| 阶段 | 状态 | 产物 | 证据 |
|---|---|---|---|
| 0 进入与基线 | `done` | `迭代说明.md` | `<起始 HEAD>` |
| 1 需求澄清 | `pending` | `01-需求澄清.md` | |
| 2 设计方案 | `pending` | `02-设计方案.md` | |
| 3 编码实现 | `pending` | `03-实现记录.md` | |
| 4 单元测试 | `pending` | `04-单元测试.md` | |
| 5 全量测试 | `pending` | `05-测试执行.md` | |
| 6 Review | `pending` | `06-审查报告.md` | |
| 7 推送 | `pending` | `07-推送记录.md` | |

## 假设与自行决策

| 项 | 采用的最小假设 | 理由 |
|---|---|---|
| `<普通实现取舍>` | `<假设内容>` | `<为何不影响业务>` |

## 未决业务问题

| 问题 | 阻塞的阶段 | 状态 |
|---|---|---|
| `<问题>` | `<阶段 N>` | `<待确认 / 已澄清>` |

## 确认关卡记录

> 确认记录是过程留痕，不是审批状态。

| 序号 | 阶段 | 时间 | 用户原话摘要 | 结论 |
|---|---|---|---|---|
| | | | | |

## 正式文档落点

| 产物 | 路径 | 状态 |
|---|---|---|
| 需求记录 | `<docs/需求/... 或「本次未建 + 原因」>` | |
| 技术方案 | `<docs/技术方案/... 或「本次未建 + 原因」>` | |
| 上线计划 | `<路径 或「由 paneasypush-requirements 决定」>` | |
| 审查报告 | `<docs/代码审查/... 或「仅 .task/ 06」>` | |
